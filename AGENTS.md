# Link168 Agent 根级规则

## 开工前必读

所有 Agent 必须按顺序读取：

1. `00_GOVERNANCE_LOCKED/PRODUCT_CONSTITUTION.md`
2. `00_GOVERNANCE_LOCKED/OWNER_DECISIONS.md`
3. `PRD.md`
4. `02_EXECUTION_CONTROL/README.md`
5. 自己在 `03_AGENT_TASKS/` 中的任务卡

## 绝对禁止

- 修改、移动、覆盖或删除 `00_GOVERNANCE_LOCKED/**`
- 自行修改产品定位、价格、会员权益、核心流程或五个一级导航
- 未经老板批准删除页面、路由、字段、模型、接口或未来能力
- 跨越任务卡规定的文件或领域边界
- 读取、打印、提交或记录任何密钥和生产连接信息
- 未经明确授权操作生产服务器、数据库、支付、邮件、AI或其他外部系统
- 伪造接口成功、测试通过、部署完成或外部服务已验证
- 覆盖其他 Agent 的工作和操作记录

## 执行要求

- 每个 Agent 只处理一个独立领域。
- 开工前检查 Git 状态和任务边界。
- 发现共享文件、规则冲突或需要删除时停止相关修改并报告总控。
- 每次工作都在 `05_OPERATION_LOGS/agent-XX/` 新增记录，不覆盖旧记录。
- 日志必须列出修改文件、命令、测试结果、风险和未完成事项。
- 代码存在不等于完成；验收以 `06_ACCEPTANCE/` 中的证据为准。
- 未明确授权的行为默认不执行。

## 冲突优先级

老板最新明确决定 > 产品宪法 > PRD > 当前任务卡 > 历史文档 > 旧代码行为。
