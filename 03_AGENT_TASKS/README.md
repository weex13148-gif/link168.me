# 八 Agent 正式任务卡区

规则：

1. 每个 Agent 只能执行自己的任务卡。
2. 任务卡必须写明目标、输入、文件边界、禁止范围、交付物、验收和停止条件。
3. 开发前由 Agent 01 确认本轮文件所有权；无任务卡不得写代码。
4. 存在共享文件或顺序依赖的任务不得并发写入。
5. 任务完成后必须提交操作记录，不得直接宣布整体完成。

唯一生效任务卡为 `AGENT_01.md` 至 `AGENT_08.md`。旧十 Agent 规划卡只保存在 `07_ARCHIVE/AGENT_TASKS_10_AGENT_DRAFT/`，不得继续调用。
