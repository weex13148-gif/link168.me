# Link168 P0 最终验证记录

日期：2026-07-02

## 验证范围

本次验证基于当前最新 `master`，覆盖：

- AI Credits 原子扣减、失败退回、幂等流水和余额不足拦截；
- 支付宝异步回调地址公网 HTTPS 校验；
- 支付宝 `alipay.trade.query` 主动查单；
- 支付宝查单原始响应 RSA2 验签；
- 管理员手工查单、手工补单和批量漏单检查；
- 受 `PAYMENT_RECONCILE_SECRET` 保护的定时补单端点；
- 支付宝回调、查单和补单诊断日志；
- 超级管理员支付测试工具；
- AI Credits 发放、消费和退回流水审计页面；
- 新版 V1 用户后台与当前收费代码的兼容性。

## 自动检查

该验证分支只增加本记录文件，不修改业务代码；Pull Request 自动检查用于验证当前 `master` 的完整代码状态。

检查项目：

- Prisma Validate；
- Prisma Generate；
- ESLint；
- TypeScript；
- Next.js Production Build；
- 生产构建完整性检查。

## 边界

自动构建通过不代表真实支付宝环境已经验收。以下内容仍需在公网生产或测试环境执行：

- 支付宝应用审核和电脑网站支付签约；
- 真实密钥和 Seller ID；
- 0.01 元真实支付；
- 公网异步回调；
- 模拟回调丢失后的主动补单；
- 会员生效和 AI Credits 到账；
- 成功 AI 调用扣减 1 Credit；
- 失败 AI 调用自动退回；
- 真实邮件、浏览器和服务器部署验收。
