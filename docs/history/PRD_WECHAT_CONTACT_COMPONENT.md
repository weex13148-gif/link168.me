# Link168 微信联系组件 PRD

**版本：** v1.0  
**日期：** 2026-07-12  
**状态：** 老板批准，供下一开发批次执行  
**适用分支：** `codex/link168-v2-direction`  
**上位规则：** `PRODUCT_CONSTITUTION.md`、`PRD.md`

> 本文件只定义公开经营名片中的“微信联系组件”。它不是微信登录、微信账号绑定、企业微信组织接入或通讯录同步。上述身份与协作平台能力仍按主 PRD 延后。

---

## 1. 一句话目标

让访客在 Link168 公开名片中，通过“微信号复制 + 二维码查看/保存 + 可选外链尝试跳转”的可靠路径联系名片主人，并把相关行为沉淀为真实的联系方式点击与客户线索信号。

---

## 2. 产品定位

Link168 的微信联系能力不是普通外链按钮，而是国内经营名片的标准转化组件：

```text
微信号展示
+ 一键复制
+ 微信二维码
+ 操作指引
+ 可选外链尝试跳转
+ 行为统计
+ AI客服联动
+ 线索沉淀
```

对外统一名称：**微信联系**。

禁止使用以下误导性文案：

- 一键自动加好友
- 100%打开微信
- 自动添加微信
- 已成功加好友

---

## 3. 参考项目的有效蒸馏

只吸收成熟交互和组件思路，不整仓复制，不引入旧技术栈，不改变 Link168 模块化单体架构。

| 公开项目 | 仅吸收的有效部分 |
|---|---|
| `metanoia1989/PersonalBusinessCard` | 名片字段编辑、实时预览、二维码与分享入口 |
| `gooking/visitingCard` | 微信场景中的联系方式首屏、复制与分享操作 |
| `roobe/jiatui` | 销售型名片的强联系入口、产品与客户线索关系 |
| `psoho/fast-poster` | 头像、文字、二维码组成分享海报的元素模型 |
| `vercel/chatbot` | AI回答中渲染结构化联系卡片的交互方式 |

执行规则：

- 开发前检查参考仓库许可证；未确认许可不得复制源码。
- 只参考交互、状态和数据结构，不复制品牌资产、页面视觉和业务代码。
- 微信图标使用项目统一合法图标源，不上传来源不明的大品牌素材。

---

## 4. 用户故事

### 4.1 名片主人

我可以在名片后台添加微信号和二维码，选择展示样式，预览后发布，并随时关闭或替换。

### 4.2 访客

我可以复制微信号、查看或保存二维码，并得到清楚的添加步骤；即使当前浏览器无法打开微信，也不会中断联系流程。

### 4.3 企业经营者

我可以看到微信组件曝光、复制、二维码查看和外链点击数据，并把高意向行为用于客户跟进。

### 4.4 AI客服

当访客询问联系方式、合作或报价时，AI只能展示名片主人已公开启用的微信联系组件，不能猜测或读取未公开信息。

---

## 5. V1 开发范围

### 5.1 必做能力

1. 新增“微信联系”名片组件。
2. 支持填写微信号、显示名称、辅助说明。
3. 支持上传、替换、删除微信二维码。
4. 支持一键复制微信号。
5. 支持二维码大图预览。
6. 支持保存二维码图片。
7. 支持“简洁按钮”和“联系卡片”两种样式。
8. 移动端使用底部抽屉，桌面端使用居中弹层。
9. 复制成功后展示明确添加步骤。
10. 可配置“尝试打开微信”或用户提供的合法外部联系链接。
11. 跳转失败时回退到复制微信号和二维码。
12. 记录真实行为事件，不伪造加好友成功。
13. AI客服可渲染已发布的微信联系卡。
14. 编辑器、手机预览和公开页使用统一组件渲染器。
15. 组件数据必须按 `workspaceId`、`profileId` 和权限隔离。

### 5.2 暂不开发

- 微信注册、登录和账号绑定
- 普通个人微信加好友官方 API
- 自动搜索、自动添加、自动发送消息
- 判断访客是否真实加为好友
- 读取微信聊天记录
- 企业微信通讯录或成员同步
- 微信群自动入群
- 第三方跳转平台和来源不明私有协议
- 多客服轮询分配
- 重型海报编辑器

---

## 6. 后台编辑规则

入口：

```text
名片 → 添加组件 → 联系方式 → 微信联系
```

### 6.1 字段

| 字段 | 必填 | 规则 |
|---|---:|---|
| 组件标题 | 否 | 默认“微信联系”，1–30字 |
| 显示名称 | 否 | 微信昵称、姓名或客服名称，1–40字 |
| 微信号 | 条件必填 | 与二维码至少填写一个；去除首尾空格，最大64字符 |
| 辅助说明 | 否 | 如“产品咨询与合作”，最大80字 |
| 验证备注 | 否 | 如“请备注来自Link168”，最大40字 |
| 工作时间 | 否 | 纯展示文本，最大40字 |
| 二维码 | 条件必填 | 与微信号至少提供一个 |
| 外部联系链接 | 否 | 必须经过服务端 URL 校验 |
| 按钮文案 | 否 | 默认“微信联系”，最大20字 |
| 展示微信号 | 是 | 默认开启 |
| 允许复制 | 是 | 有微信号时默认开启 |
| 允许保存二维码 | 是 | 有二维码时默认开启 |
| 尝试打开微信 | 是 | 默认关闭，失败必须有兜底 |
| 样式 | 是 | `BUTTON` 或 `CONTACT_CARD` |
| 启用状态 | 是 | 新建默认关闭，保存并主动启用后公开 |

### 6.2 二维码上传

- 支持 JPG、PNG、WebP。
- 最大 2MB。
- 建议正方形且不低于 600×600。
- 服务端校验 MIME、扩展名、文件大小和实际图片内容。
- 不覆盖旧文件；替换成功后再清理旧文件。
- 上传失败不能保存无效地址。
- 公开启用前明确提示：二维码会向所有可访问该名片的人展示。

### 6.3 编辑状态

- 空状态：引导填写微信号或上传二维码。
- 未启用：只在后台和预览中显示，不在公开页出现。
- 配置不完整：禁止启用并指出缺失项。
- 上传失败：保留原二维码，不显示伪成功。
- 保存失败：保留当前编辑内容，允许重试。

---

## 7. 公开页交互

### 7.1 简洁按钮

```text
[微信图标] 微信联系
```

适合 Link-in-bio 简洁主题。点击后打开联系弹层。

### 7.2 联系卡片

```text
微信联系
阿宝 · Link168
产品咨询、合作与报价
微信号：weex13148
[复制微信号] [查看二维码]
```

适合经营、销售和企业名片，为默认推荐样式。

### 7.3 联系弹层

必须包含：

- 标题和显示名称
- 微信号（按配置决定是否展示）
- 复制微信号按钮
- 二维码（存在时）
- 查看/保存二维码按钮
- 验证备注和工作时间（存在时）
- 尝试打开微信或合法外链按钮（存在且启用时）
- 关闭按钮

复制成功反馈：

```text
微信号已复制
请打开微信 → 右上角“+” → 添加朋友 → 粘贴微信号搜索
```

跳转失败反馈：

```text
当前浏览器未能打开微信。微信号已复制，你也可以保存二维码后在微信中识别。
```

### 7.4 移动端要求

- 360–430px 不得横向溢出。
- 底部抽屉适配安全区。
- 二维码不得超出视口。
- 长微信号可换行或省略，但复制内容必须完整。
- 按钮触控高度不低于 44px。
- 弹层打开后锁定背景滚动。

---

## 8. 外链与跳转规则

优先级：

```text
用户提供并通过校验的合法外部联系链接
→ 尝试打开微信客户端
→ 复制微信号
→ 展示/保存二维码
```

规则：

- 不自行拼接来源不明的私有协议参数。
- 不把“打开尝试”记录为“已添加好友”。
- 外链必须使用允许的 `https` 地址或经项目安全白名单批准的 scheme。
- 禁止 `javascript:`、`data:`、恶意重定向、短链套娃和任意脚本执行。
- 外链打开使用安全属性，防止反向控制原页面。
- 未配置外链时，不显示空按钮。

---

## 9. 数据模型建议

优先复用现有统一名片组件模型；只有现有模型无法表达时才新增专用表。禁止同时维护两套真相源。

建议组件配置：

```ts
type WechatContactConfig = {
  title: string;
  displayName?: string;
  wechatId?: string;
  description?: string;
  verificationNote?: string;
  businessHours?: string;
  qrCodeUrl?: string;
  externalContactUrl?: string;
  buttonText: string;
  showWechatId: boolean;
  allowCopy: boolean;
  allowQrDownload: boolean;
  tryOpenWechat: boolean;
  layout: "BUTTON" | "CONTACT_CARD";
};
```

若采用专用模型，至少包含：

```text
id
workspaceId
profileId
componentId
configJson
enabled
sortOrder
createdAt
updatedAt
```

所有读写同时校验：

```text
当前用户
+ workspaceId
+ profileId
+ 名片编辑权限
```

---

## 10. 行为事件与数据口径

必须记录：

```text
wechat_component_view
wechat_contact_open
wechat_copy_id
wechat_qr_view
wechat_qr_save
wechat_open_attempt
wechat_external_link_click
```

不得记录为事实：

```text
wechat_friend_added
wechat_contact_success
```

除非未来取得可靠、合规、可验证的官方结果回传。

统计口径：

- 曝光：组件真实进入视口后记一次。
- 打开：访客主动打开弹层。
- 复制：浏览器剪贴板写入成功后记录。
- 保存：下载动作真实触发后记录。
- 尝试跳转：只代表用户点击尝试。
- 去重规则沿用现有访问与组件点击统计体系。

---

## 11. AI客服联动

触发意图包括：

- 怎么联系你
- 加微信
- 想合作
- 获取报价
- 发方案
- 详细聊聊

AI处理规则：

1. 查询当前公开名片是否存在已启用微信联系组件。
2. 只返回该组件已公开字段。
3. 使用结构化联系卡渲染，不把二维码 URL 当普通文本直接输出。
4. 未配置时回答“当前未公开微信联系方式”，不得推测。
5. 不读取其他名片、其他 Workspace 或后台隐藏信息。
6. 联系卡查看、复制等行为按同一事件体系记录。
7. 可将“询问合作 + 打开微信联系卡”标记为高意向信号，但不能自动标记成交。

---

## 12. 隐私、安全与风控

- 微信号和二维码默认不公开；用户主动启用后才展示。
- 页面明确提示公开风险。
- 二维码文件使用受控上传与访问路径。
- 禁止在日志中记录完整二维码文件内容。
- 举报原因增加“联系方式诈骗/诱导”。
- 对二维码和组件接口实施访问限流。
- 防止通过接口枚举其他用户未发布的微信信息。
- 组件关闭或名片下线后，公开 API 不再返回配置。
- 删除组件后按现有文件清理策略处理二维码。
- 不把微信号用于平台营销或其他未经授权用途。

---

## 13. 套餐边界

V1 建议：

- 免费版：1 个微信联系组件，基础按钮/卡片，基础点击统计。
- Plus：高级样式、辅助说明、工作时间、90天统计、AI联系卡联动。
- Pro：自定义主题适配、365天分析、来源与转化分析。
- 企业版：成员名片微信联系、企业品牌样式、企业客户池联动。

最终权益必须走现有服务端权益真相源，前端不能自行判断或写死套餐。

---

## 14. 开发顺序

### 阶段 A：只读确认

- 检查现有组件模型、渲染器、上传服务、点击统计和 AI 工具结构。
- 确认是否能复用，不先创建新表。
- 检查编辑器、预览、公开页是否共用渲染底座。

### 阶段 B：核心组件

- 配置校验与保存。
- 二维码上传、替换、删除。
- 按钮和卡片渲染。
- 联系弹层、复制、查看和保存。

### 阶段 C：统计与 AI

- 接入真实行为事件。
- 接入 AI 结构化联系卡工具。
- 接入线索意向信号，不自动创建虚假客户。

### 阶段 D：验收

- 类型检查、静态检查、组件测试、接口测试、生产构建。
- 360、390、430px 真机或浏览器验证。
- 编辑器、手机预览、公开页逐项一致性比对。

---

## 15. 验收标准

全部满足才算完成：

1. 用户可添加微信号或二维码，至少一个存在才能启用。
2. 二维码上传失败不会覆盖原图或返回假成功。
3. 公开页只展示已发布且启用的组件。
4. 复制成功后提供明确的微信添加步骤。
5. 无法打开微信时仍可复制或扫码联系。
6. 编辑器、预览和公开页内容与样式规则一致。
7. 360、390、430px 无横向溢出和遮挡。
8. 微信号和二维码不会跨用户、跨名片或跨 Workspace 泄漏。
9. 所有外链经过服务端安全校验。
10. 统计只记录曝光、打开、复制、查看、保存和点击，不伪造好友添加结果。
11. AI仅展示已公开启用的微信信息。
12. 免费与付费权益由服务端真实校验。
13. 组件关闭、删除或名片下线后，公开接口立即停止返回。
14. lint、typecheck、相关测试和生产构建通过。

---

## 16. 明日开发边界

明日只开发本 PRD 的 V1 范围，不顺带开发：

- 微信登录或绑定
- 企业微信连接器
- 微信群
- 完整海报设计器
- 新的一级导航
- 多 Agent 重构
- 与微信组件无关的数据库扩张

若现有代码结构与本 PRD 冲突，先提交只读差异报告，由主 Agent 按产品宪法裁决；不得自行扩大范围或删除已有兼容结构。
