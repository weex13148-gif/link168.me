# Link168 媒体上传统一规则 PRD

**版本：** v1.0  
**日期：** 2026-07-12  
**状态：** 老板批准，供下一开发批次执行  
**适用分支：** `codex/link168-v2-direction`  
**上位规则：** `PRODUCT_CONSTITUTION.md`、`PRD.md`

> 本文件作为 Link168 图片与媒体上传的统一规则来源。当前 P0 先落实用户头像；企业 Logo、AI 助手头像、客服头像、名片封面、背景图、产品图、案例图和二维码后续按同一底座复用。禁止各模块重复实现不同上传逻辑。

---

## 1. 一句话目标

建立统一、安全、可复用的媒体上传能力，保证用户头像上传、裁剪、压缩、替换、缓存刷新、删除和多端展示一致，不再由各页面各写一套实现。

---

## 2. 当前 P0 范围

本轮必须完成：

- 用户头像上传
- 用户头像裁剪
- 用户头像替换
- 用户头像删除与默认头像回退
- 编辑器、手机预览、公开名片、用户后台统一显示
- 文件安全校验
- 图片压缩与尺寸生成
- 缓存更新
- 旧文件清理
- 权限与 Workspace 隔离

本轮暂不开发：

- 滤镜、美颜、贴纸
- 在线图片设计器
- AI 修图
- 任意角度旋转
- GIF 动图头像
- SVG 头像
- 多人协同编辑图片
- 独立媒体中心一级导航

---

## 3. 统一适用对象

以下资源必须逐步接入同一媒体底座：

```text
用户头像
企业 Logo
企业成员头像
AI 助手头像
AI 客服头像
微信联系头像
公开名片封面
公开名片背景图
产品图片
服务图片
案例图片
二维码图片
分享海报图片
```

禁止：

- 每个模块单独写上传接口
- 每个页面使用不同大小限制
- 同一头像在多个表中维护多个真相源
- 上传成功后只更新某一个页面
- 直接暴露服务器真实文件路径

---

## 4. 用户头像上传规则

### 4.1 支持格式

允许：

- JPG
- JPEG
- PNG
- WebP

暂不支持：

- GIF
- SVG
- HEIC
- BMP
- TIFF
- AVIF

### 4.2 文件限制

- 最大文件：2MB
- 最小尺寸：200 × 200
- 建议尺寸：800 × 800
- 最大可接受原图：4096 × 4096
- 宽高任一超过上限时必须拒绝或服务端安全缩放
- 不允许空文件、损坏文件或无法解码的图片

错误文案必须直接、中文、可操作：

```text
图片过大，请上传小于 2MB 的 JPG、PNG 或 WebP 图片。
```

```text
图片尺寸过小，请上传至少 200 × 200 的图片。
```

```text
无法识别这张图片，请更换后重试。
```

---

## 5. 上传与处理流程

正式流程：

```text
用户选择图片
→ 前端仅做基础预检
→ 上传到服务端
→ 服务端校验 MIME、文件头、尺寸和实际可解码性
→ 去除 EXIF 与无关元数据
→ 进入裁剪流程
→ 生成标准方形头像
→ 生成多尺寸资源
→ 保存新媒体记录
→ 原子更新用户头像引用
→ 刷新缓存
→ 各端显示新头像
→ 异步清理旧头像
```

原则：

- 前端校验不能替代服务端校验。
- 只有新资源完整生成并保存成功后，才能替换旧头像。
- 任一步失败都必须保留原头像。
- 不得出现“数据库已更新但图片不可访问”的半成功状态。
- 不得伪造上传成功。

---

## 6. 裁剪规则

上传后进入统一裁剪界面。

支持：

- 拖动图片
- 缩放图片
- 居中裁剪
- 圆形预览
- 正方形成品

暂不支持：

- 任意比例裁剪
- 自由旋转
- 滤镜
- 美颜
- 贴纸
- 文字叠加

规则：

- 最终存储图像为正方形。
- UI 可使用圆形头像展示，但底层资源不得只生成圆形透明图。
- 裁剪区域不得超出原图有效范围。
- 裁剪完成前不得修改当前头像。
- 取消裁剪时恢复原头像状态。

---

## 7. 标准输出尺寸

用户头像至少生成：

```text
96 × 96
160 × 160
320 × 320
640 × 640
```

建议用途：

| 尺寸 | 用途 |
|---|---|
| 96 | 列表、小头像、消息区域 |
| 160 | 用户后台、联系卡片 |
| 320 | 公开名片常规展示 |
| 640 | 高分屏、分享、后续导出 |

规则：

- 默认输出 WebP。
- 原始图片不直接用于公开页面。
- 前端根据场景和设备选择合适尺寸。
- 不允许所有页面都加载 640 × 640。
- 图片不得拉伸、压扁或失真。

---

## 8. 默认头像规则

用户未上传头像、头像被删除或资源加载失败时，必须显示稳定的默认头像。

优先级：

```text
用户姓名首字母或首个汉字
→ 系统统一默认头像
```

禁止：

- 裂图
- 空白区域
- 404 图片
- 随机第三方头像
- 未经授权自动使用社交平台头像

默认头像在编辑器、手机预览、公开页和后台必须保持一致。

---

## 9. 替换与删除规则

### 9.1 替换

- 新头像处理完成前保留旧头像。
- 新头像成功写入后再更新数据库引用。
- 使用新版本 URL 或版本参数避免旧缓存长期存在。
- 替换后编辑器、手机预览、公开页和后台同步刷新。
- 旧文件进入延迟清理队列，不在事务内强制同步删除。

### 9.2 删除

删除头像后：

```text
清空当前头像引用
→ 立即恢复默认头像
→ 公开页和后台同步更新
→ 原头像进入垃圾回收
```

规则：

- 删除操作必须二次确认。
- 删除失败时保留原头像。
- 不允许删除其他用户头像。
- 企业管理员不能修改或删除成员私人空间头像，除非产品规则明确授权。

---

## 10. 缓存与刷新规则

头像更新后必须避免浏览器持续显示旧资源。

建议方案：

```text
不可变媒体 URL
+ 新文件新路径或版本号
+ 数据库更新新 URL
```

规则：

- 不依赖用户手动清理浏览器缓存。
- 上传成功后 5 秒内各主要页面应显示新头像。
- 公开页、编辑器和预览不能使用不同头像来源。
- API 返回新头像后，前端查询缓存必须同步失效或更新。
- 不允许直接覆盖同一路径后期待 CDN 自动立即刷新。

---

## 11. 安全规则

服务端必须：

- 校验 MIME Type
- 校验文件扩展名
- 校验文件头与真实格式
- 实际解码图片
- 限制文件大小
- 限制像素总量
- 去除 EXIF 和定位信息
- 随机化文件名
- 防止路径穿越
- 防止脚本伪装成图片
- 禁止 SVG XSS
- 禁止公开访问临时上传文件
- 不在日志中记录文件二进制内容
- 不暴露服务器绝对路径

高风险情况必须拒绝：

- MIME 与文件头不一致
- 图片解码失败
- 超大像素攻击
- 文件内容含脚本或不可识别数据
- 上传来源无权限
- 跨 Workspace 修改资源

---

## 12. 权限与归属

### 12.1 用户头像

只有以下主体可修改：

- 头像所属用户本人
- 经产品规则明确授权的系统超级管理员

普通企业管理员不得修改成员私人账号头像。

### 12.2 企业资源

企业 Logo、企业成员名片头像和企业客服头像必须同时校验：

```text
workspaceId
+ 当前用户角色
+ 资源归属
+ 编辑权限
```

### 12.3 禁止行为

- 通过猜测资源 ID 修改他人头像
- 访问未发布或无权限的原始图片
- 把个人头像自动复制到其他 Workspace
- 把企业资源归到个人账户

---

## 13. 数据模型建议

优先建立统一媒体资源模型，不为头像、Logo、产品图分别维护完全独立上传逻辑。

建议结构：

```text
MediaAsset
- id
- ownerUserId
- workspaceId nullable
- resourceType
- storageKey
- mimeType
- originalWidth
- originalHeight
- sizeBytes
- checksum
- status
- createdAt
- updatedAt
- deletedAt nullable
```

标准变体：

```text
MediaVariant
- id
- mediaAssetId
- variantName
- width
- height
- format
- storageKey
- sizeBytes
```

头像业务表只保存当前媒体引用：

```text
User.avatarMediaId
```

或沿用现有字段但确保只有一个正式真相源。

执行前必须先检查现有 Prisma 模型和上传代码，能复用则复用，不允许无审计地重复建表。

---

## 14. API 与错误状态

建议接口职责：

```text
POST   /api/media/avatar/upload
POST   /api/media/avatar/confirm
DELETE /api/media/avatar
GET    /api/media/:id/variant/:name
```

具体路由应按现有项目结构复用，不强制照抄命名。

API 必须返回真实状态：

- 400：格式、大小、尺寸或裁剪参数错误
- 401：未登录
- 403：无权限
- 404：资源不存在
- 409：资源状态冲突
- 413：文件过大
- 422：图片无法处理
- 429：上传过于频繁
- 500：服务端处理失败

前端不得把失败响应显示成上传成功。

---

## 15. 移动端要求

- 360–430px 不得横向溢出。
- 上传入口可直接调用相册或相机选择。
- 裁剪器必须适配安全区。
- 缩放与拖动操作不与页面滚动冲突。
- 按钮触控高度不低于 44px。
- 上传中、处理中、成功、失败状态清楚可见。
- 网络中断后允许重新选择或重试。
- 上传进度不要求伪造百分比；无法取得真实进度时显示处理中状态。

---

## 16. 开发顺序

### 阶段 A：只读确认

- 检查现有头像字段、上传接口、文件存储和删除逻辑。
- 检查编辑器、预览、公开页和后台当前头像来源。
- 检查是否已有缓存版本、文件清理和尺寸处理。
- 输出复用清单与冲突点，禁止直接另起一套。

### 阶段 B：统一上传底座

- 服务端文件校验
- 图片解码、压缩和多尺寸生成
- 媒体记录与归属
- 新旧头像原子替换
- 旧文件延迟清理

### 阶段 C：统一前端交互

- 上传入口
- 裁剪器
- 加载、失败、重试和删除状态
- 默认头像
- 编辑器、预览、公开页同步

### 阶段 D：验收

- 类型检查
- 静态检查
- 上传接口测试
- 权限测试
- 图片格式与恶意文件测试
- 移动端 360、390、430px 验证
- 生产构建

---

## 17. 验收标准

全部满足才算完成：

1. 用户可以上传 JPG、PNG、WebP 头像。
2. 超过 2MB、低于 200 × 200 或不可解码的图片被真实拒绝。
3. MIME、扩展名和文件头不一致的文件无法上传。
4. 用户可拖动和缩放完成正方形裁剪。
5. 系统生成 96、160、320、640 四档头像。
6. 图片自动去除 EXIF 和定位信息。
7. 新头像成功前旧头像保持可用。
8. 替换后 5 秒内编辑器、手机预览、公开页和后台显示一致。
9. 删除头像后所有页面恢复同一默认头像。
10. 非法用户无法修改或删除他人头像。
11. 不同 Workspace 资源无法串读串写。
12. 旧头像进入清理流程，不无限堆积。
13. 页面不出现裂图、404 或空白头像。
14. 360、390、430px 上传与裁剪无横向溢出。
15. 上传失败、删除失败和处理失败均不伪造成功。
16. lint、typecheck、相关测试和生产构建通过。

---

## 18. 明日开发边界

明日只优先落实“用户头像上传统一规则”，不得顺带扩张为：

- 完整素材库
- 重型图片编辑器
- AI 生图或修图
- 独立媒体一级导航
- 与头像无关的大规模数据库重构
- 删除现有品牌、产品、二维码或上传能力

若现有代码已经具备部分能力，必须增量统一和复用，不重复开发、不覆盖其他 Agent 正在修改的代码。
