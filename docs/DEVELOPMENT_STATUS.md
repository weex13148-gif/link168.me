# Link168 开发状态

更新：2026-09-05。本文仅是恢复开发的进度入口，不是新的产品规格。

## 唯一施工入口

- 目录：`C:\Users\bifuc\.codex\worktrees\a056\link1688`
- 分支：`codex/controlled-clean-rebuild-20260814`
- 本轮开始时 HEAD：`bf13f4bc137ab273460a7685afb94dcc08c9c253`；下次进入必须重新读取，不回退到此 SHA。
- 产品权威：`../00_CURRENT_GUIDANCE_INDEX.md` 指向的现役合同。
- 执行计划：`superpowers/plans/2026-08-15-current-mvp-targeted-completion.md`。
- 逐项历史证据：同计划 `.superpowers/sdd/2026-08-15-current-mvp-targeted-completion/progress.md`（本地执行记录）。

## 当前进度

| 范围 | 状态 | 证据／下一步 |
|---|---|---|
| Task 1 基线审计 | 历史已完成 | 原 ledger 保留，不重新派发 |
| Task 2 个人初始化 | 历史已实现 | `2f1f333..9f93085`；本轮待真实运行验证 |
| Task 3 页面列表 | 历史已实现 | `876e69d`；本轮 typecheck 基线通过 |
| Task 4 团队、邀请、成员页 | 代码已实现；数据库验收待完成 | 团队控制台、持久邀请、原子加入、成员页及权限边界已接通；静态与匿名运行检查通过 |
| Task 5 生命周期 | 未完成本轮验收 | 旧服务存在，转让确认及关闭边界仍需核对 |
| Task 6–13 | 后续待接通 | 按原计划继续，不删除范围、不重新建立重建计划 |
| Task 14 完整交付验收 | 未完成 | 代码、数据库、浏览器、Provider 分开记录 |

## 本轮验证与环境

- 开始时工作区干净，分支匹配。
- `node node_modules/typescript/bin/tsc --noEmit --incremental false`：基线通过。
- `CURRENT_AUTHORITY_CHECKSUMS.sha256`：开始时 24/24 匹配。
- R2 原始清单：7 份现役 Markdown 与原始版本不同，为既有迭代；不改写原始清单掩盖差异。
- 未连接或修改 DB-1、生产数据库或真实 Provider；没有发布。
- 后续检查记录真实命令和结果，不把历史 PASS 当本轮结果。

## 防止上下文和副本失控

1. 保留完整产品目标；日常开发直接沿现有任务接续。
2. 普通开发不新建分支、worktree、恢复副本或平行产品文档。
3. 分支身份与时间以 Git 为准；旧副本只作证据，不从旧代码反推当前需求。
4. 旧计划里的临时限制保留其日期和上下文；不拷入下一轮当永久权限。
5. 每次结束更新本表中的当前任务、实际验证与下一步；无法验证的项保持待核验。
6. 清理副本另行核对未提交、未跟踪、忽略文件和数据库；不得用工作区干净或提交 SHA 不同判断可删除。

## 阻塞分类

- **代码缺口**：继续开发，记录具体接口／行为。
- **环境缺口**：明确缺数据库、服务配置或测试账号；不阻止无关开发。
- **未验收**：需要真实操作证明，不伪造结果。
- **生产操作**：部署、真实数据变更和支付等须单独确认，不能沿用本地开发授权。

## 2026-09-05 收尾验证

- 全量 ESLint、Prisma validate/generate、Next webpack 构建（含类型检查和 152 个静态页面）通过。构建 sitemap 有占位数据库不可用警告，不能视为数据库验收。
- standalone 资源打包和本地 127.0.0.1:32168 启动通过。
- 24 项纯源码策略检查通过，覆盖成员权限、席位、邀请返回路径和保留用户名；不代表数据库集成验证。
- 4 项 HTTP 检查通过：匿名团队列表 401、缺失邀请 404、匿名加入 401、跨站加入 403。缺失邀请最初检查误预期 400，经源码核对应为 NOT_FOUND/404 后修正检查，未改变 API。
- 浏览器确认邀请缺 token 显示错误、匿名团队控制台跳转登录、登录/注册切换保留邀请返回路径；未注册或使用真实账号。
- diff-check 通过，修正 schema 末尾多余空行。现役权威校验最终 24/24 匹配。
- 隔离 PostgreSQL 初始化受沙箱 restricted token/re-exec 权限阻止；未初始化数据库、未执行迁移、未验证数据库事务。未连接 DB-1、生产库或真实 Provider。

## 接续位置

- 团队服务 src/lib/current/team/page-service.ts；API src/app/api/current/teams/；管理页面 src/app/console/team/；邀请入口 src/app/invite/team/。
- 邀请令牌仅存哈希、7 天到期、可撤销；接受时重新验证邀请人角色和席位，失败事务回滚。
- 成员页写入/发布验证允许字段和 Team Offering；失效成员不能继续公开访问；超席位阻止写入/发布。
- 重新加入保留历史版本，但清除旧发布指针，必须重新明确发布。
- 下一步在授权隔离库验证：创建团队、并发席位、重复/过期/撤销邀请、邀请人降权、移除/重新加入、公开页可见性、失败回滚。通过前 Task 4 不计完整验收。
- 可独立继续 Task 5 生命周期：所有权转移双方确认及团队关闭；Task 6–14 继续原计划。
- 本轮修改尚未提交；下次先读 git status，不能从旧 staging 目录重新复制覆盖。没有新增分支、worktree、备份或删除旧副本，没有提交、推送或发布。
