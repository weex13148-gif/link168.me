# Link168 功能报告（摘要版）

日期：2026-07-02  
正式仓库：`weex13148-gif/link168.me`  
唯一正式分支：`master`  
功能基线：`715e0391025b85c2f18fefac208e7cc2fcae9974`

## 总体结论

Link168 已完成 GitHub 代码层面的核心闭环：邮箱注册登录、邮箱验证、新版用户后台、免费无限链接、公开主页、二维码分享、五档会员、支付宝下单、异步回调开通会员、AI 额度账户和付费 AI 权限。

当前仍未完成真实支付宝 0.01 元付款、公网异步回调、真实邮件全流程、浏览器人工回归、本地正式项目同步和腾讯云生产部署。因此当前正确状态是：

> GitHub 代码闭环完成，下一阶段为真实接口验收和生产部署。

## 会员方案

- 免费版：¥0，1 个公开主页、无限链接、基础二维码和免费主题，AI 不产生真实调用。
- Plus：¥188/年。
- Pro：¥388/年。
- 企业会员：¥1288/年。
- 企业专业 Plus：¥3988/年。
- 历史 `member_basic` 按 Plus 权益兼容。
- 超级管理员保留 0.01 元 `internal_test` 套餐。

## 用户后台

正式入口：`/dashboard`

功能区：我的主页、名片资料、我的链接、主题装修、分享与二维码、账户与安全。

已完成：头像、名称、简介、公开地址、无限链接、新增/编辑/删除、公开/隐藏、排序、真实保存状态、主题服务端保存、二维码、邮箱验证、修改密码、登录设备、手机端更多入口和可折叠实时预览。

免费链接规则已经统一：前端和服务端均为无限链接，不再限制 10 个。

## 公开主页

已完成 `/[username]`、主题与布局、只显示公开链接、直接访问原始 URL、产品展示、举报入口、历史用户名保留和跳转、邮箱未验证/冻结/封禁状态页。

## 账户与邮件

已完成注册限流、密码哈希、Session、6 位验证码、重新发送、验证确认、忘记密码、重置密码、修改密码和多设备退出。注册超过 30 天未验证邮箱时，公开主页可暂停；未验证邮箱禁止购买会员。

## 支付宝收费

当前只开放支付宝，微信支付明确提示后续开放；只允许年付。订单接口兼容前后端字段命名，金额按分保存、按元展示；下单前自动创建 AI 额度账户；支付成功后依赖服务端异步回调开通会员和发放权益。

超级管理员入口：`/jeepwork/settings/payment`。

真实上线前必须完成 0.01 元订单、付款、验签、异步回调、订单更新、会员生效、AI 额度发放和重复回调幂等测试。

## AI

真实百炼调用允许套餐：`member_basic`、`member_plus`、`pro`、`enterprise`、`enterprise_pro_plus`、`internal_test`。免费用户不得产生真实 AI 调用。

当前服务端实际定义的是五个 Agent：财税、法务、市场调研、设计、社媒运营。业务规划中的销售顾问 Agent 尚未进入正式服务端定义，不能写成六大 Agent 已完成。

## 超级管理员与比赛

已完成真实数据概览、用户管理、验证状态、套餐角色、限制冻结、日志与 IP、举报、邮件和 AI 配置、支付宝与收费、比赛页面控制及访问日志。

`/showcase` 默认关闭，由超级管理员开启并使用共享密码控制；普通首页不提供入口。

## GitHub 版本管理

- `master` 是唯一正式版本和部署来源。
- 当前没有开放中的 Pull Request。
- 历史 CI、审计、比赛、内测包和功能分支不是独立产品版本。
- 版本规则见 `docs/REPOSITORY_VERSION_POLICY.md`。

可清理的临时分支包括 `ci/*`、`audit/*`、旧 `release/*`、比赛检查分支以及已经合并的 Dashboard 功能分支。删除前应确认没有尚未进入 `master` 的唯一业务代码。

## 上线前 P0

1. 同步本地 `D:\link168\link.me` 到 `origin/master`。
2. 执行 Prisma Validate、Prisma Generate、TypeScript 和 Production Build。
3. 完成邮箱注册、验证和找回密码真实测试。
4. 完成支付宝 0.01 元公网闭环。
5. 完成手机、笔记本和宽屏浏览器回归。
6. 创建服务器快照并部署腾讯云。
7. 检查数据库、PM2、Nginx、HTTPS 和上传目录。
8. 完成生产冒烟和回滚演练。

## 后续范围

销售顾问 Agent、完整统一知识库、访客 AI 客服、完整双语内容、独立域名绑定、更多主题、微信登录、微信支付、短信和企业成员权限不属于当前已验收的 P0 生产闭环。
