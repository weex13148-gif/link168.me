# Link168 AI 名片头像、产品头像与素材库 PRD

**版本：** v1.0  
**日期：** 2026-07-12  
**状态：** 老板批准，供下一开发批次执行  
**适用分支：** `codex/link168-v2-direction`  
**上位规则：** `PRODUCT_CONSTITUTION.md`、`PRD.md`、`PRD_MEDIA_UPLOAD_RULES.md`

> 本文件补充 AI 名片头像、产品头像、常用平台 Logo、自定义图标和素材库规则。它不替代统一媒体上传规则，而是在其上增加名片经营场景的素材选择、品牌合规、图标管理和复用能力。

---

## 1. 一句话目标

让 Link168 用户无需每次重新上传图片，即可从“我的素材、系统图标、常用平台 Logo、最近使用”中快速选择头像、产品图片和链接图标，同时允许上传自定义图标，并确保品牌、版权、权限和公开展示安全。

---

## 2. 产品范围

本轮包含：

```text
AI 名片头像
+ 产品头像/产品封面
+ 常用中国平台 Logo
+ 通用业务图标
+ 自定义图标上传
+ 我的素材库
+ 最近使用
+ 素材搜索与分类
+ 编辑器、预览、公开页统一渲染
```

本轮不包含：

- 完整在线设计器
- AI 生图与 AI 修图
- 版权素材商城
- 自动抓取第三方网站 Logo
- 自动使用用户社交账号头像
- 未经授权的大品牌宣传素材包
- 视频素材编辑
- 团队素材审批流
- 独立一级导航

---

## 3. 核心原则

1. **一套媒体底座**：头像、产品图、Logo、自定义图标和二维码统一复用媒体上传、压缩、权限、缓存与删除能力。
2. **一种资源一个真相源**：页面只保存素材引用，不复制多份 URL 和文件。
3. **先选后传**：编辑时优先展示可直接使用的素材库，再提供上传入口。
4. **国内使用习惯优先**：系统图标优先覆盖微信、电话、地图、电商、内容平台和本地生活场景。
5. **不冒充官方**：平台 Logo 只用于指示跳转目标，不表示 Link168 与平台存在合作、认证或授权关系。
6. **不复制来路不明素材**：Logo、图标和模板必须有明确来源、许可证或品牌使用依据。
7. **移动端优先**：选择、搜索、预览、裁剪和替换在 360–430px 正常使用。

---

## 4. AI 名片头像规则

### 4.1 头像来源

用户可从以下来源选择：

- 上传新头像
- 我的素材
- 最近使用
- 系统默认头像
- 企业品牌素材（有权限时）

不得自动从微信、企业微信、抖音、小红书或其他第三方平台抓取头像。

### 4.2 头像处理

沿用统一媒体上传规则：

- JPG、PNG、WebP
- 最大 2MB
- 最低 200×200
- 正方形裁剪
- 去除 EXIF
- 生成 96、160、320、640 多尺寸
- 替换成功后再清理旧资源
- 编辑器、手机预览、公开页和后台同步刷新

### 4.3 AI 名片场景补充

- 名片头像默认采用圆形展示，底层仍存正方形资源。
- 模板可切换圆形、圆角方形，但不得改变原始资源。
- AI 客服头像与名片头像默认分离，用户可主动选择“使用名片头像”。
- 企业成员名片可使用个人头像或企业统一头像模板，但不能覆盖成员私人账号头像。
- 删除头像后回退到姓名首字或系统默认头像。

---

## 5. 产品头像与产品封面规则

### 5.1 定义

产品组件支持两类图片：

- **产品头像**：小尺寸方形图，适合列表和紧凑卡片。
- **产品封面**：横向或方形主图，适合大卡片、详情和分享。

两者可以引用同一原图的不同裁剪变体，也可以分别配置。

### 5.2 上传要求

允许：JPG、PNG、WebP。

- 产品头像：建议 800×800，最小 300×300，最大 5MB。
- 产品封面：建议 1200×900 或 1200×1200，最小短边 600px，最大 5MB。
- 服务端必须进行真实格式、文件头、尺寸、像素总量与可解码性校验。
- 去除 EXIF、定位和无关元数据。

### 5.3 输出尺寸

产品头像建议生成：

```text
160 × 160
320 × 320
640 × 640
```

产品封面建议生成：

```text
480px 宽
960px 宽
1440px 宽
```

具体尺寸可根据现有图片组件统一，但不得公开页直接加载原始大图。

### 5.4 空状态

未上传产品图片时：

- 可选择系统通用行业图标作为临时头像。
- 可显示产品名称首字或统一占位图。
- 不得出现裂图、404 或随机外部图片。
- 占位图不能误导为真实商品图片。

### 5.5 编辑规则

```text
上传图片
从我的素材选择
从最近使用选择
使用通用业务图标
裁剪
替换
删除
```

替换失败必须保留旧图；删除后回退到明确占位状态。

---

## 6. 常用中国平台 Logo 库

### 6.1 使用定位

常用平台 Logo 只用于：

- 链接组件图标
- 联系方式组件图标
- 平台跳转按钮
- 分享渠道说明
- 后台渠道识别

不得用于：

- 冒充平台官方页面
- 宣称官方合作或授权
- 制作误导性认证标志
- 替代用户自己的企业 Logo
- 大面积品牌背景装饰

### 6.2 第一批建议收纳

#### 联系与社交

- 微信
- 企业微信
- QQ
- 微博
- 知乎
- 即刻

#### 内容与短视频

- 抖音
- 快手
- 小红书
- 哔哩哔哩
- 视频号
- 公众号

#### 电商与交易

- 淘宝
- 天猫
- 京东
- 拼多多
- 闲鱼
- 得物
- 微店
- 有赞

#### 本地生活与出行

- 美团
- 大众点评
- 高德地图
- 百度地图
- 滴滴
- 携程
- 飞猪

#### 办公与业务

- 钉钉
- 飞书
- 腾讯会议
- 腾讯文档
- 石墨文档
- 金山文档

#### 支付与经营

- 支付宝
- 微信支付
- 云闪付

### 6.3 上架规则

每一个品牌 Logo 上架前必须记录：

```text
品牌名称
官方来源或合法图标源
来源链接或许可证
允许用途
禁止用途
图标版本
更新时间
```

优先顺序：

1. 品牌官方公开资源与品牌规范。
2. 具备明确许可证的图标库。
3. 自行重绘的通用“平台类型图标”，不得冒充官方 Logo。

无法确认来源或使用规则时，不收录官方 Logo，改用通用图标。

### 6.4 展示规则

- 保持官方 Logo 的比例和识别特征。
- 不随意改色、拉伸、旋转、描边或叠加文字。
- 深浅背景下使用品牌允许的适配版本；无适配版本时放在中性图标容器中。
- 不把多个品牌 Logo 拼成 Link168 自己的品牌图。
- 页面底部增加必要说明：平台名称及商标归各自权利人所有，Link168 仅用于链接识别。

---

## 7. 通用业务图标库

当用户没有合适图片或不适合使用品牌 Logo 时，提供统一通用图标。

第一批分类：

### 联系方式

- 电话
- 短信
- 邮箱
- 微信联系
- 企业微信联系
- 地址
- 地图
- 营业时间
- 保存联系人

### 经营行为

- 产品
- 服务
- 价格
- 优惠
- 预约
- 报价
- 咨询
- 下单
- 支付
- 下载资料

### 内容

- 图文
- 视频
- 直播
- 文章
- 课程
- 案例
- 相册
- 文件
- 二维码

### 行业

- 餐饮
- 零售
- 教育培训
- 咨询服务
- 设计
- 摄影
- 房产
- 汽车
- 装修
- 健身
- 美业
- 法律
- 财税
- 软件与互联网

规则：

- 使用同一图标体系和统一视觉粗细。
- 默认支持跟随主题色。
- 不混用多套风格差异过大的图标。
- 图标名称和搜索关键词使用中文，并补充常见同义词。

---

## 8. 自定义图标规则

### 8.1 使用场景

用户可以为以下内容上传自定义图标：

- 普通链接
- 自定义联系方式
- 产品或服务分类
- 行动按钮
- 企业内部渠道

### 8.2 格式与限制

允许：

- PNG
- JPG
- WebP

SVG 默认不允许普通用户直接上传，避免脚本和外链风险。后续如支持，必须服务端净化并转换成安全输出格式。

规则：

- 最大 1MB
- 最低 64×64
- 建议 256×256
- 最大 1024×1024
- 自动裁剪为正方形安全区域
- 生成 64、128、256 三档资源
- 去除 EXIF 和透明区异常数据

### 8.3 合规声明

上传时用户必须确认：

> 我确认拥有该图片或图标的合法使用权，不侵犯他人商标、著作权和其他权益。

平台应提供举报与下架机制。

---

## 9. 素材库信息架构

素材库不新增一级导航，入口放在：

```text
名片编辑器 → 选择图片/图标
产品编辑 → 选择产品图片
我的 → 素材管理
企业空间 → 品牌素材（企业权限）
```

素材选择器包含：

```text
最近使用
我的上传
系统图标
常用平台
企业素材
```

### 9.1 我的素材

用户可：

- 上传
- 搜索
- 筛选类型
- 查看使用位置
- 重命名
- 添加标签
- 删除未使用素材
- 替换引用中的素材需单独确认

### 9.2 最近使用

- 按最近被用户选择的时间排序。
- 只显示当前用户或当前 Workspace 有权访问的素材。
- 删除原素材后从最近使用中同步移除。
- 最近使用不是资源副本，不重复占用存储。

### 9.3 企业素材

企业管理员可管理：

- 企业 Logo
- 品牌标准色
- 官方产品图
- 企业通用头像
- 企业授权图标
- 统一宣传素材

普通成员只能使用被授权素材，不得删除企业全局素材。

---

## 10. 素材分类与搜索

### 10.1 分类

```text
头像
Logo
产品图
产品封面
链接图标
通用图标
二维码
背景图
封面图
案例图
其他
```

### 10.2 搜索字段

支持按以下内容搜索：

- 素材名称
- 中文关键词
- 标签
- 平台名称
- 行业名称
- 最近使用

例如：

```text
微信 / 加微信 / 社交
电话 / 手机 / 联系
淘宝 / 店铺 / 电商
餐饮 / 美食 / 门店
```

### 10.3 筛选

- 类型
- 来源
- 上传时间
- 使用状态
- 当前 Workspace
- 我的素材 / 企业素材 / 系统素材

---

## 11. 素材数据模型建议

优先复用统一 `MediaAsset` 和 `MediaVariant` 模型，补充素材库元数据，不创建多套媒体表。

建议补充：

```text
MediaAsset
- id
- ownerUserId
- workspaceId nullable
- sourceType: USER_UPLOAD | SYSTEM_ICON | BRAND_LOGO | ENTERPRISE_ASSET
- assetType: AVATAR | PRODUCT_AVATAR | PRODUCT_COVER | LINK_ICON | LOGO | QR_CODE | BACKGROUND | OTHER
- name
- storageKey
- mimeType
- width
- height
- sizeBytes
- checksum
- licenseType nullable
- licenseSource nullable
- brandName nullable
- status
- createdAt
- updatedAt
- deletedAt nullable
```

建议增加：

```text
MediaTag
MediaAssetTag
MediaUsage
RecentMediaUsage
```

`MediaUsage` 至少记录：

```text
mediaAssetId
workspaceId
resourceType
resourceId
fieldName
createdAt
```

用途：

- 防止删除仍在使用的素材。
- 查看素材被哪些名片、产品和组件引用。
- 统一替换与清理。

开发前必须检查现有 Prisma 模型，能复用则增量扩展，不无审计重复建表。

---

## 12. 素材选择器交互

### 12.1 桌面端

```text
左侧：分类
中间：素材网格
顶部：搜索与上传
右侧：预览、来源、尺寸与使用按钮
```

### 12.2 移动端

- 底部抽屉或全屏选择页。
- 顶部搜索。
- 横向分类标签可滚动，但不得影响全局五个一级导航。
- 每行 3–4 个图标或 2 个图片卡片。
- 选择后可预览并确认使用。

### 12.3 状态

- 加载中
- 空状态
- 搜索无结果
- 上传处理中
- 上传失败
- 无权限
- 素材已删除
- 素材仍在使用，无法直接删除

接口失败不能显示成空素材库。

---

## 13. 删除、替换与垃圾回收

### 13.1 删除规则

- 未被引用的用户素材可直接删除并进入垃圾回收。
- 被引用素材必须先显示使用位置。
- 用户可选择取消删除，或确认从具体组件移除后删除。
- 系统图标与平台 Logo 不能被普通用户删除。
- 企业素材只能由有权限的企业管理员删除。

### 13.2 替换规则

“全局替换素材”属于高风险操作：

- 必须展示受影响的名片、产品和组件数量。
- 必须二次确认。
- 替换失败必须保持原引用。
- 跨 Workspace 禁止替换。

### 13.3 垃圾回收

- 删除先软删除。
- 保留合理恢复期。
- 确认无引用后再删除存储文件和变体。
- 系统素材和品牌 Logo 不进入用户垃圾回收。

---

## 14. 权限与安全

所有素材操作同时校验：

```text
当前用户
+ workspaceId
+ 素材归属
+ 资源编辑权限
+ 使用场景权限
```

必须防止：

- 枚举素材 ID 读取他人私有图片
- 跨 Workspace 选用企业素材
- 普通成员删除企业品牌素材
- 通过外链上传恶意文件
- SVG 脚本执行
- 图片炸弹与超大像素攻击
- 文件名路径穿越
- 上传内容伪造 MIME

系统素材公开访问与用户私有素材管理接口必须分离。

---

## 15. 套餐边界建议

最终以服务端权益真相源为准。

### 免费版

- 使用系统通用图标
- 使用常用平台 Logo
- 上传基础头像和产品图
- 我的素材容量采用基础额度

### Plus

- 更多我的素材容量
- 最近使用
- 自定义链接图标
- 高级产品封面样式

### Pro

- 更高素材容量
- 素材标签与搜索增强
- 自定义主题图标适配
- 长期素材使用记录

### 企业版

- 企业品牌素材库
- 企业 Logo 和统一产品素材
- 成员共享与权限管理
- 企业素材使用记录

不得只在前端隐藏超额能力；上传和选择接口必须服务端校验权益。

---

## 16. 开发顺序

### 阶段 A：只读盘点

- 检查头像、产品图、链接图标、二维码和现有上传接口。
- 检查是否已有图标库、静态品牌资源和素材选择器。
- 检查许可证和来源记录。
- 输出复用清单、冲突清单和需迁移字段。

### 阶段 B：AI 名片头像与产品图统一

- 统一媒体引用。
- 增加产品头像和产品封面。
- 编辑器、预览和公开页统一渲染。
- 空状态、替换、删除和缓存更新。

### 阶段 C：系统图标与常用平台

- 建立通用业务图标库。
- 收录来源明确、用途允许的常用平台 Logo。
- 建立品牌来源与许可证记录。
- 链接组件支持选择图标。

### 阶段 D：我的素材库

- 我的上传
- 最近使用
- 搜索与分类
- 使用位置记录
- 安全删除与垃圾回收

### 阶段 E：企业素材

- 企业 Logo、产品素材和共享图标。
- 管理员与成员权限。
- 企业素材使用记录。

---

## 17. 验收标准

全部满足才算完成：

1. AI 名片头像可从上传、我的素材、最近使用中选择。
2. 产品支持产品头像和产品封面，不再只有单一模糊图片字段。
3. 编辑器、手机预览和公开页使用同一媒体引用和渲染规则。
4. 用户可以为链接选择通用图标、常用平台 Logo 或自定义图标。
5. 常用平台 Logo 均有来源或许可证记录。
6. 未确认合法来源的品牌 Logo 不进入系统库。
7. 自定义图标上传经过真实格式、安全和尺寸校验。
8. 素材库支持最近使用、我的上传、系统图标、常用平台和企业素材分类。
9. 素材可搜索、筛选和预览。
10. 删除被引用素材前展示真实使用位置并阻止误删。
11. 不同用户和 Workspace 素材无法串读、串写或误删。
12. 系统图标和平台 Logo 不可被普通用户删除或覆盖。
13. 免费与付费容量和能力由服务端权益校验。
14. 360、390、430px 的素材选择器、裁剪和预览无横向溢出。
15. 上传、替换、删除和选择失败时不伪造成功。
16. lint、typecheck、相关测试和生产构建通过。

---

## 18. 开发边界

本轮只做对 Link168 经营名片直接有价值的素材能力，不顺带开发：

- Canva 类设计器
- AI 生图平台
- 版权图片商城
- 自动抓取全网 Logo
- 社交平台 OAuth 头像同步
- 独立第六个一级导航
- 与素材无关的大规模架构重构

如现有代码已经有头像、产品图片或图标实现，必须增量统一和迁移，不覆盖其他 Agent 正在修改的代码，不删除历史兼容能力。
