# Link168 产品宪法入口

> 本文件仅作为历史路径兼容入口，不包含第二份宪法正文。

当前唯一有效的产品宪法位于：

`00_GOVERNANCE_LOCKED/PRODUCT_CONSTITUTION.md`

执行规则：

1. 所有 Agent 开工前必须读取锁定目录中的产品宪法。
2. `00_GOVERNANCE_LOCKED/**` 默认禁止 Agent 修改、移动、覆盖或删除。
3. 只有老板明确书面授权后，治理负责人才能按 `CHANGE_PROTOCOL.md` 修改。
4. PRD、任务卡、代码与宪法冲突时，停止冲突部分并提交老板裁决。
5. 完整性标记位于 `00_GOVERNANCE_LOCKED/CONSTITUTION_BLOB_SHA`。
