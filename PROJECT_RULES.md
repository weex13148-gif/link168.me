# Link168 工程、Agent、安全与部署规则

**文件名：** `PROJECT_RULES.md`  
**版本：** v1.0-rc2  
**生成日期：** 2026-07-10  
**状态：** 根据产品宪法和当前仓库生成，待老板批准后生效  
**适用范围：** TRAE、Codex、开发子Agent、产品文档Agent、服务器Agent、部署机器人和人工开发者

---

## 1. 规则优先级

所有工程执行必须遵守：

```text
老板最新明确决定
→ PRODUCT_CONSTITUTION.md
→ PRD.md
→ PROJECT_RULES.md
→ 代码、数据库、页面、测试与部署结果
```

`PROJECT_RULES.md` 不是产品最高规则，无权覆盖产品宪法或 PRD。

当代码与PRD冲突时整改代码；当PRD与产品宪法冲突时整改PRD；当历史文档与正式文档冲突时历史文档失效。

---

## 2. 正式仓库与工作区

- GitHub：`weex13148-gif/link168.me`
- 当前正式 V2 分支：`codex/link168-v2-direction`
- 正式本地工作区：`D:\77.me\branches\link168-v2-direction`
- 旧 `D:\link168\link.me`、旧工作区、审计副本和归档分支均只读保留。
- `master` 与当前 V2 分支为独立历史，未经老板批准不得强行合并、rebase 或覆盖。
- 部署源必须在部署任务中明确指定，禁止从旧目录或归档分支部署。

---

## 3. 正式文档

正式权威文档只保留：

```text
仓库根目录/
├─ PRODUCT_CONSTITUTION.md
├─ PRD.md
├─ PROJECT_RULES.md
└─ DOCUMENT_INDEX.md
```

临时分析、Agent过程记录和测试输出不得继续堆放在正式文档目录。

- 长期过程记录：`03_AGENT_RECORDS/`
- 历史文档：统一归档目录
- 临时报告：任务结束后删除或集中归档
- 不得自动生成大量重复日报、审计报告和说明性Markdown

修改权限：

- 产品宪法：老板或明确授权的产品文档Agent
- PRD：老板或明确授权的产品文档Agent
- 工程规则：老板或明确授权的架构/工程Agent
- 文档索引：文档负责人，修改后同步版本和状态

---

## 4. Agent职责边界

### 4.1 项目总控Agent

负责读取正式文档、拆分任务、分配模块和文件边界、汇总结果、识别冲突和组织验收。

不得自行改变产品方向、套餐结构、删除未来业务结构或获取生产密钥。

### 4.2 产品文档Agent

只负责产品宪法授权范围内的 PRD、信息架构、用户流程、验收口径和文档一致性。

未经授权不得修改代码、数据库和生产配置。

### 4.3 开发Agent

每次必须有明确模块、目标和文件边界。不得默认全仓修改。

发现跨模块问题时先报告，不得顺手大面积重构。

### 4.4 构建验收Agent

负责 TypeScript、ESLint、生产构建、关键测试、差异检查和最终验收。

开发过程中采用最小必要检查；阶段收口和上线前执行完整检查，避免每个小改动重复全量构建。

### 4.5 服务器Agent / 部署机器人

只执行明确授权的服务器任务，不负责产品决策和自由架构设计。

不得自行安装 Docker、MongoDB、MySQL、OpenClaw 或其他无关软件。

不得输出密钥值、修改密钥、删除数据或改变数据库目标。

---

## 5. 修改与冲突控制

每个任务必须声明：

- 目标模块
- 允许修改的文件或目录
- 禁止修改的范围
- 数据库是否允许改动
- 是否允许新增依赖
- 验收方式

多个Agent并行时：

- 不得同时修改同一文件。
- 不得覆盖其他Agent未合并改动。
- 总控Agent负责冲突收口。
- 发现规则冲突时记录条款、文件、影响和建议，提交老板裁决。

---

## 6. 删除与保留原则

默认采用：

> 保留优先、功能开关限制、删除需老板批准。

删除页面、路由、字段、模型、支付、会员、企业、AI、知识库、推广、卡密或未来扩展结构前，必须提供：

- 删除清单
- 删除原因
- 依赖关系
- 数据影响
- 迁移或兼容方案
- 老板批准记录

未完成能力优先隐藏入口、关闭功能、限制权限或降级，不得为当前收口方便直接删除。

Showcase相关数据库结构目前仍存在于 schema。未经专项评估和老板批准，不得直接删除迁移或生产表。

---

## 7. 密钥与配置安全

绝不提交：

- `.env`、`.env.local`、`.env.production`
- 数据库密码
- 会话密钥
- 配置加密密钥
- 管理员密钥
- 邮件凭据
- 支付私钥、公钥原文和商户密钥
- AI Provider密钥
- OSS/存储密钥
- Token、密码和私有证书

规则：

- 前端不得持有敏感密钥。
- 真实AI、邮件和支付调用必须经过服务端。
- 第三方业务API由超级管理员受控配置中心管理。
- `DATABASE_URL`、`SESSION_SECRET`、`CONFIG_ENCRYPTION_KEY` 等基础设施密钥保留在服务器环境变量。
- 日志只允许显示配置是否存在，不得显示值。

---

## 8. 数据库规则

- 数据库事实以 `prisma/schema.prisma` 和 migration 为准。
- 生产环境只允许 `prisma migrate deploy`，禁止 `prisma migrate reset`。
- 数据库修改前先只读确认数据库名、用户、schema、migration状态和连接目标。
- 不得手工修改生产表来代替正式migration。
- 不得清空用户、订单、线索、AI流水、审计日志和企业数据。
- 数据模型删除必须考虑历史migration和生产数据兼容。
- 当前数据库服务器与网站服务器分离，禁止默认数据库与网站在同一台机器。

用户会自行设置服务器和数据库快照。Agent不得以“再备份一次”为理由擅自创建额外备份，但仍必须确认快照或回滚边界已由老板声明。

---

## 9. Git规则

未经明确授权不得：

- force push
- 删除正式分支或Tag
- 改写历史
- 合并独立历史
- 修改分支保护
- 创建大量临时分支
- 自动提交、推送或发起PR

正常任务中，只有老板明确要求“提交、推送、上传或建PR”后才能执行写操作。

执行Git写操作前必须先只读确认：

- 当前目录
- 当前分支
- HEAD
- remote
- `git status`
- `git diff`
- 网络与远端可达性

---

## 10. 测试与构建

开发阶段采用省算力模式：

- 单模块修改：执行与模块直接相关的静态检查或最小测试。
- 阶段收口：执行 typecheck 和关键业务检查。
- 最终验收/上线前：执行 lint、typecheck、生产build和核心端到端验收。

不得：

- 为每个小改动重复全量审计和构建
- 用假数据冒充功能测试
- 未配置真实第三方服务却声称支付、邮件或AI已通过
- 为追求“全绿”擅自删除业务功能

---

## 11. 用户可见产品规则

普通用户页面不得出现：

- 开发中
- 待接入
- 即将开放
- Demo
- Mock
- 测试功能
- Agent报告
- 临时页面
- 技术债说明

未完成能力应隐藏、禁用、降级或仅在内部报告说明。

设计必须遵守移动端优先、“艺术感+商业感”、编辑预览公开页一致和核心转化入口清晰。

---

## 12. 权限与业务规则

- 所有关键权限必须服务端校验。
- 免费版不赠送套餐内AI额度、不自带六大AI Agent；购买有效AI加量包后，可按该SKU的适用范围、额度和有效期调用对应AI能力。
- 企业成员不得访问其他企业数据。
- 企业管理员不得访问平台后台能力。
- 普通用户导航不得展示Jeepwork管理入口。
- 页面层不得直接实现订单、套餐、额度和会员核心规则。
- 套餐、权限、AI额度、订单状态、企业租户和推广归属必须有唯一服务端来源。

---

## 13. 部署流程

部署分两步。

### 第一步：只读环境确认

只确认，不写入、不上传、不覆盖、不重启、不改配置、不改数据库：

- 服务器目录
- 部署源分支与commit
- 待上传文件清单
- 排除 `.md`、docs、reports、audits、PRD和无关资料
- PM2进程
- 应用端口
- Nginx反向代理
- 数据库连接目标
- migration状态
- 环境变量名称是否齐全
- 构建产物和磁盘空间

### 第二步：老板明确批准后部署

- 直接覆盖正式程序文件
- 安装正式依赖
- 执行批准的migration
- 构建
- 重启PM2
- 验证本机健康检查与公网核心页面

部署不得上传正式产品运行不需要的Markdown、审计报告、PRD、比赛资料和历史文档。

---

## 14. 上线后检查

上线后至少连续3天人工检查：

- 首页
- 注册、登录、找回密码、邮箱验证
- `/console`及核心子页面
- 名片编辑、手机预览和公开页
- 产品、线索、数据、二维码和短链接
- 会员、订单、支付状态与退款入口
- AI权限、套餐额度与加量包校验
- Jeepwork与管理员权限
- 手机端适配、错误提示、数据保存和页面稳定性

发现问题记录实际页面、账号角色、时间、接口状态和复现步骤，不以“代码看起来正确”代替产品验收。
