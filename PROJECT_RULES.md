# Link168 工程、Agent、安全与部署规则

**文件名：** `PROJECT_RULES.md`  
**版本：** v1.0-rc5
**更新日期：** 2026-07-12  
**状态：** 持续执行  
**适用范围：** TRAE、Codex、开发子Agent、产品文档Agent、服务器Agent、部署机器人和人工开发者

---

## 1. 规则优先级

```text
老板最新明确决定
→ 00_GOVERNANCE_LOCKED/PRODUCT_CONSTITUTION.md
→ PRD.md
→ PROJECT_RULES.md
→ 代码、数据库、页面、测试与部署结果
```

本文件无权覆盖产品宪法或PRD。代码与PRD冲突时整改代码；PRD与宪法冲突时整改PRD。

---

## 2. 正式仓库与工作区

- GitHub：`weex13148-gif/link168.me`
- 当前受治理V2分支：`codex/link168-v2-governed-workspace`
- 建议本地工作区：`D:\77.me\branches\link168-v2-governed-workspace`
- 旧 `D:\link168\link.me`、旧工作区、审计副本和归档分支只读保留。
- 未经老板批准，不得合并独立历史、rebase、force push或从旧目录部署。

---

## 3. 正式文档

正式权威文档只保留：

```text
00_GOVERNANCE_LOCKED/PRODUCT_CONSTITUTION.md
PRD.md
PROJECT_RULES.md
DOCUMENT_INDEX.md
```

长期整改状态统一记录在：

```text
docs/audits/REMEDIATION_DEVELOPMENT_REPORT.md
```

不得自动制造大量重复日报、阶段报告和临时Markdown。

修改权限：

- 产品宪法和PRD：老板或明确授权的产品文档Agent。
- 工程规则：老板或明确授权的架构/工程Agent。
- 文档索引：文档负责人，修改后同步版本与状态。

---

## 4. 八 Agent 固定职责

Link168开发固定使用以下八个正式岗位，禁止AI自行增加、拆分、合并或改名：

1. 总控架构与集成 Agent
2. 产品规则与需求治理 Agent
3. 身份权限与企业空间 Agent
4. 商业主页与前端体验 Agent
5. 客户线索与访问分析 Agent
6. AI接待与知识库 Agent
7. 会员支付与平台治理 Agent
8. 质量安全与发布 Agent

详细职责、文件边界和并发规则以 `02_EXECUTION_CONTROL/EIGHT_AGENT_OPERATING_MODEL.md`、`02_EXECUTION_CONTROL/FILE_OWNERSHIP_MATRIX.md` 和 `03_AGENT_TASKS/AGENT_01.md` 至 `AGENT_08.md` 为准。

### 4.1 总控架构与集成 Agent

读取正式文档、拆分任务、分配文件边界、识别冲突、汇总结果和组织验收。

不得自行改变产品方向、套餐结构、AI购买资格、企业边界或生产架构。

### 4.2 产品规则与需求治理 Agent

维护产品宪法、PRD、文档索引和持续整改报告。未经授权不得修改业务代码、数据库和生产环境。

### 4.3 领域开发 Agent

每次任务必须声明：

- 目标模块
- 允许修改范围
- 禁止修改范围
- 是否允许数据库变更
- 是否允许新增依赖
- 验收方式

不得默认全仓修改，不得覆盖其他Agent未合并代码。

### 4.4 质量安全与发布 Agent

开发过程中执行最小必要检查；阶段收口和上线前执行完整typecheck、lint、生产构建和核心验收。

不得用假数据、假支付、假AI结果冒充通过。

### 4.5 服务器Agent

只执行明确授权的服务器任务，不负责产品决策和自由架构设计。

不得自行安装Docker、MongoDB、MySQL、OpenClaw或其他无关软件；不得输出、修改密钥或改变数据库目标。

---

## 5. 并行与冲突控制

- 多个Agent不得同时修改同一文件。
- 不得覆盖其他Agent改动。
- 总控Agent负责冲突收口。
- 发现产品、代码或数据库冲突时，记录条款、文件、影响和建议，提交老板决定。
- 不得自行修改宪法适应旧代码。

---

## 6. 删除与保留

默认规则：

> 保留优先、功能开关限制、删除需老板批准。

删除页面、路由、字段、模型、会员、支付、AI、AI加量包、企业、知识库、推广、卡密或未来结构前，必须提供：

- 删除清单
- 删除原因
- 依赖关系
- 数据影响
- 迁移或兼容方案
- 老板批准记录

Showcase相关历史数据库模型和迁移不得由Agent直接删除。

---

## 7. 密钥与配置

严禁提交或输出：

- `.env*`
- 数据库密码
- `SESSION_SECRET`
- `CONFIG_ENCRYPTION_KEY`
- 管理员密钥
- 邮件凭据
- 支付私钥和商户密钥
- AI Provider密钥
- OSS/存储密钥
- Token、密码和私有证书

规则：

- 前端不得持有敏感密钥。
- AI、邮件和支付必须经服务端调用。
- 第三方业务API密钥由Jeepwork受控配置中心管理。
- 基础设施密钥保留在服务器环境变量。
- 日志只能显示配置是否存在，不得显示值。

---

## 8. 数据库

- 数据库事实以 `prisma/schema.prisma` 和migration为准。
- 生产只允许 `prisma migrate deploy`，禁止 `prisma migrate reset`。
- 修改前只读确认数据库名、用户、schema、migration和连接目标。
- 不得手工改生产表代替migration。
- 不得清空用户、订单、线索、AI流水、点数批次、佣金、审计和企业数据。
- 数据模型删除必须考虑历史migration和生产数据兼容。
- 网站服务器和数据库服务器分离，禁止默认二者位于同一台机器。
- 用户负责设置服务器和数据库快照；Agent不得擅自创建额外备份，但必须确认回滚边界已声明。

---

## 9. Git

未经老板明确授权不得：

- force push
- 删除正式分支或Tag
- 改写历史
- 合并独立历史
- 修改分支保护
- 创建大量临时分支
- 自动提交、推送或发起PR

执行Git写操作前，只读确认目录、分支、HEAD、remote、status、diff和远端可达性。

---

## 10. 测试与省算力规则

- 单模块修改：执行相关静态检查或最小测试。
- 阶段收口：执行typecheck和关键业务检查。
- 最终验收/上线前：执行lint、typecheck、生产build和核心端到端验收。

不得：

- 每个小改动重复全量审计和构建
- 无真实配置却声称支付、邮件或AI通过
- 为追求全绿删除业务功能
- 用页面展示代替服务端权限验收

---

## 11. 用户可见产品规则

普通用户页面不得出现：

- 开发中
- 待接入
- 即将开放
- Demo
- Mock
- 测试功能
- Agent报告
- 临时页面
- 技术债说明

未完成能力应隐藏、禁用或降级。

设计必须移动端优先，保持“艺术感+商业感”，并确保编辑器、手机预览和公开页一致。

---

## 12. 权限与业务规则

- 所有关键权限必须服务端校验。
- 免费版不赠送AI额度、不自带六大AI Agent，也不允许购买AI加量包。
- `/console/ai-packs` 和加量包订单API必须拒绝免费用户。
- Plus、Pro可以购买个人AI加量包。
- 企业AI加量包只能由企业所有者或管理员购买，普通成员只能消费分配额度。
- 每笔加量包独立90天有效，按最早到期批次优先消耗。
- 套餐额度、购买额度和企业共享额度必须分账。
- 失败回补必须回到原额度来源和原批次。
- 企业成员不得访问其他企业数据。
- 企业管理员不得访问Jeepwork平台能力。
- 普通用户导航不得展示Jeepwork入口。
- 套餐、权限、AI额度、点数批次、订单、佣金、企业租户和推广归属必须有唯一服务端来源。

---

## 13. 推广结算工程规则

- 支付成功后佣金进入30天锁定期。
- 锁定期内不得提现。
- 退款、拒付、撤销或风控异常不得产生可提现佣金。
- 上月已结束锁定期且满足条件的佣金，于次月15日进入统一结算；节假日顺延。
- 最低提现金额为¥50，不足金额滚入下一周期。
- 已结算订单发生退款时，生成追缴、负佣金或后续抵扣记录。
- 佣金状态、变更原因和操作人必须审计。

---

## 14. 部署流程

### 第一步：只读确认

只确认，不上传、不覆盖、不重启、不改配置、不改数据库：

- 服务器目录
- 部署源分支和commit
- 待上传文件清单
- 排除 `.md`、docs、reports、audits、PRD和无关资料
- PM2进程、端口和Nginx
- 数据库连接目标和migration
- 环境变量名称
- 构建产物和磁盘空间

### 第二步：老板批准后执行

- 直接覆盖正式程序文件
- 安装正式依赖
- 执行批准的migration
- 构建
- 重启PM2
- 验证本机健康检查和公网核心页面

部署不得上传正式运行不需要的Markdown、审计报告、PRD、比赛资料和历史文档。

---

## 15. 上线后检查

上线后至少连续3天人工检查：

- 首页、注册、登录、找回密码和邮箱验证
- `/console`核心页面
- 名片编辑、手机预览和公开页
- 产品、线索、数据、二维码和短链
- 会员、订单、支付宝、退款
- AI权限、免费用户购买拦截、加量包90天批次、扣点和回补
- 推广佣金锁定、结算和最低提现
- Jeepwork和管理员权限
- 手机端适配、错误提示、数据保存和页面稳定性

不得以“代码看起来正确”代替真实产品验收。
