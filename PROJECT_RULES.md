# Link168 工程与项目执行规则

> 文档名称：PROJECT_RULES  
> 版本：v1.0.0  
> 状态：正式执行版  
> 生效日期：2026-07-11  
> 上位文档：`PRODUCT_CONSTITUTION.md`、`PRD.md`  
> 适用范围：研发、测试、设计协作、AI Agent、GitHub、数据库、部署与运维

---

## 0. 文档定位

本文件只规定 Link168 的工程、安全、协作、测试、Git、数据库和部署规则。

本文件不得：

- 改变产品定位；
- 改变五个一级入口；
- 改变套餐结构；
- 改变免费版与 AI 的边界；
- 把轻量客户模块扩张为完整 CRM；
- 覆盖产品宪法和正式 PRD；
- 将历史比赛流程重新变成真实产品开发的默认主线。

产品方向由产品宪法决定，当前功能与验收由 PRD 决定，本文件负责保证实施过程安全、可追踪、可验证。

---

# 第一章 文档与决策优先级

## 1.1 正式优先级

发生冲突时，严格按照以下顺序执行：

```text
老板最新明确决定
→ PRODUCT_CONSTITUTION.md
→ PRD.md
→ PROJECT_RULES.md / ADR / 设计系统
→ 代码与数据库现状
→ 历史 PRD、审计报告、比赛资料和讨论记录
```

任何 Agent 不得把本文件解释成高于产品宪法或 PRD。

## 1.2 开发前阅读顺序

开始产品或代码任务前，至少阅读：

1. `DOCUMENT_INDEX.md`；
2. `PRODUCT_CONSTITUTION.md`；
3. `PRD.md`；
4. 当前有效的一致性审计或任务说明；
5. 本文件；
6. 与任务相关的 ADR、模块文档和代码。

## 1.3 历史文档

以下内容默认仅供追溯，除非老板明确重新启用：

- `docs/archive/**`；
- 历史比赛日报和 Agent 台账；
- 已被新版本替代的 PRD；
- 旧审计报告；
- 临时 CI 触发文件；
- 旧部署记录；
- 与当前宪法冲突的架构或商业方案。

历史资料不要求随意删除，但必须避免被误认为当前执行规则。

## 1.4 决策不明确时

若正式文档与老板当前指令仍不能确定具体实现：

- 先保护数据和已有可用能力；
- 选择可回滚、可配置、低破坏性的方案；
- 明确记录假设；
- 不得自行改变产品定位或商业规则；
- 不得用“代码已经这样”代替产品决策。

---

# 第二章 Git 与分支规则

## 2.1 基本原则

- 所有正式变更必须进入 Git；
- 不在生产服务器长期手改业务代码；
- 不提交密钥、数据库文件、构建缓存和用户上传文件；
- 每次提交应具有清晰范围；
- 不将多个无关任务混入同一提交；
- 推送前检查工作区、差异和目标分支。

## 2.2 主分支保护

未经老板明确授权，不得：

- force push `master`；
- 重写公开历史；
- 删除正式 Release Tag；
- 删除他人正在使用的分支；
- 修改 Branch Protection；
- 修改仓库协作者权限；
- 将未验证代码直接覆盖主分支。

## 2.3 开发分支

- 新任务优先在独立功能分支执行；
- 分支名称应说明目的；
- 已有正式任务分支时，不重复创建竞争分支；
- 不为简单文档更新制造大量临时分支；
- 合并前应明确 base、head、提交范围和验证结果。

## 2.4 工作区保护

发现工作区存在与当前任务无关的未提交改动时：

- 不得擅自覆盖；
- 不得默认 `git add -A`；
- 不得擅自 stash、reset、clean 或删除；
- 只暂存当前任务明确涉及的文件；
- 需要处理冲突时先列出影响范围。

## 2.5 提交与 PR

每个正式 PR 应说明：

- 修改了什么；
- 为什么修改；
- 用户或开发影响；
- 是否涉及数据库、支付、AI、权限或部署；
- 执行了哪些检查；
- 哪些事项仍未验证；
- 是否需要数据迁移或回滚。

不允许只写“优化”“修复若干问题”等无法审计的说明。

---

# 第三章 密钥与敏感信息

## 3.1 绝不提交的内容

不得进入 Git、Markdown、截图、前端 Bundle、公开日志或聊天输出：

- `.env`、`.env.local`、`.env.production`；
- 数据库密码和完整连接凭据；
- `SESSION_SECRET`、管理员秘密；
- AI Provider API Key；
- 支付应用私钥；
- 邮件服务密钥；
- OSS/S3 Access Key；
- SSH 私钥；
- 完整 Session Token；
- 其他生产凭据。

## 3.2 环境变量

- 密钥只能通过安全环境变量、服务器配置或密钥管理系统提供；
- 可以验证变量是否存在，但不得打印完整值；
- `NEXT_PUBLIC_*` 只能放公开信息；
- 任何需要密钥的调用必须经过服务端；
- `.env.example` 只能包含占位符。

## 3.3 日志

日志不得记录：

- 密码；
- API Key；
- 支付私钥；
- 完整 Cookie 或 Token；
- 不必要的完整 IP；
- 不必要的客户联系方式；
- 用户上传的完整敏感资料。

必要的诊断字段应遮罩、哈希或使用内部 ID。

## 3.4 密钥泄露处理

一旦发现密钥可能泄露：

1. 停止继续传播；
2. 记录影响范围；
3. 立即轮换凭据；
4. 检查 Git 历史和日志；
5. 修复泄露入口；
6. 验证旧密钥失效；
7. 形成安全事件记录。

删除文件但不轮换密钥不算完成修复。

---

# 第四章 代码修改原则

## 4.1 渐进式修改

大型重构应拆分为可验证的小步骤：

- 先确认现状；
- 建立目标边界；
- 小范围修改；
- 执行模块检查；
- 再进入下一步；
- 最后进行集中回归。

不得为追求“干净”一次性替换整套系统。

## 4.2 保留优先

对以下内容默认保留：

- 未来付费功能；
- 企业结构；
- AI 接口；
- 路由；
- 数据库字段；
- 审计记录；
- 订单和额度流水；
- 合规页面；
- 管理后台能力。

未使用不等于无价值。删除前必须：

1. 列出文件、路由、字段或表；
2. 搜索引用；
3. 说明运行影响；
4. 说明数据影响；
5. 说明替代方案；
6. 获得明确批准。

## 4.3 功能开关与降级

尚未正式开放的能力应优先采用：

- 服务端功能开关；
- 套餐权限；
- 角色权限；
- 内测白名单；
- 禁用状态；
- 联系销售；
- 规划中提示；
- 安全失败。

不得通过删除未来结构实现短期收口。

## 4.4 不伪造成功

以下情况必须返回真实状态：

- 邮件没有发送；
- AI 没有真实调用；
- 支付没有确认；
- 文件没有生成；
- 数据没有保存；
- 外部系统没有完成操作；
- 生产环境没有验证。

Mock、沙箱、本地成功和代码结构存在，不得表述为生产已成功。

## 4.5 业务规则集中化

以下规则应有单一服务端来源：

- 套餐与价格；
- 公开可购买套餐；
- 会员权益；
- AI 访问和额度；
- 企业权限；
- 支付订单状态；
- 用户冻结状态；
- 管理员角色；
- 客户状态字典。

不得在多个页面复制不同常量。

---

# 第五章 数据库规则

## 5.1 禁止操作

在生产环境严禁未经批准执行：

- `prisma migrate reset`；
- DROP DATABASE；
- DROP TABLE；
- TRUNCATE；
- 无条件批量 DELETE；
- 手工改表替代 migration；
- 使用开发库命令破坏生产 migration 历史。

## 5.2 Schema 变更

数据库结构变更必须：

1. 明确业务目的；
2. 评估现有数据；
3. 创建 Prisma migration；
4. 在本地或测试数据库验证；
5. 执行 `prisma validate`；
6. 执行 `prisma generate`；
7. 评估回滚或补偿；
8. 生产只使用 `prisma migrate deploy`。

## 5.3 数据迁移

涉及字段语义、状态或所有权变化时，必须说明：

- 旧值如何映射；
- 空值如何处理；
- 失败如何恢复；
- 是否需要后台兼容；
- 是否影响 API；
- 是否影响历史订单、会员和审计。

## 5.4 数据不可静默删除

以下数据不得因用户降级、会员到期、重构或功能下线静默删除：

- 订单；
- 支付记录；
- AI 额度流水；
- 客户线索和跟进；
- 管理员审计；
- 举报与处置记录；
- 企业操作记录；
- 用户历史经营数据。

需要删除时应按照产品、隐私和合规规则执行。

## 5.5 租户隔离

所有个人和企业业务查询必须明确所有权：

- 个人数据按 user/profile 范围；
- 企业数据按 workspace 范围；
- 管理员数据按角色和操作目的；
- 不得相信前端传入的 userId 或 workspaceId；
- 服务端必须验证当前操作者是否属于该空间。

---

# 第六章 认证与权限

## 6.1 服务端鉴权

以下能力必须服务端校验：

- 登录用户；
- 用户数据所有权；
- 企业成员与角色；
- 会员权益；
- AI 权限；
- 支付订单；
- 文件访问；
- `/jeepwork`；
- 超级管理员操作。

前端隐藏按钮不构成权限控制。

## 6.2 管理后台

- `/jeepwork` 仅允许平台 admin 和 super_admin；
- 企业管理员不能进入平台后台；
- 高敏感信息仅 super_admin 或明确授权角色可见；
- 管理员操作应记录审计；
- 不允许普通管理员修改系统账号或超级管理员关键权益；
- 不允许删除最后一名超级管理员。

## 6.3 Session 与 Cookie

- 使用 HttpOnly；
- 正式 HTTPS 使用 Secure；
- 根据场景选择合理 SameSite；
- 登录、密码重置和验证码需要限流；
- 退出登录使会话失效；
- 管理员和普通用户后台应保持明确边界。

---

# 第七章 AI 工程规则

## 7.1 免费用户边界

- 免费用户不得产生真实 AI 调用；
- 免费用户不得通过 Credit 绕过会员准入；
- 免费用户不得购买 AI 加量包；
- 页面展示、API 和额度服务必须同时执行规则；
- 真实调用消耗主页经营者或当前授权空间的额度。

## 7.2 统一资料库

六大助手应使用统一资料库底座，并根据 Agent 加载：

- 专属提示词；
- 工具权限；
- 风险提示；
- 模板；
- 输出格式。

个人资料和企业资料必须隔离。

## 7.3 额度

- 扣减在服务端完成；
- 使用幂等键；
- 并发不得产生负余额；
- Provider 失败按原来源补偿；
- 额度流水可审计；
- 页面余额不作为最终事实；
- 加量包不能替代有效会员资格。

## 7.4 Provider

- AI Provider 未配置时安全失败；
- 不把 Key 存入用户业务表；
- 不在客户端调用需要密钥的接口；
- Provider 错误映射为统一错误；
- 不记录完整敏感 Prompt；
- 模型供应商应可替换。

## 7.5 高风险助手

财税、法务等助手必须：

- 添加辅助性质说明；
- 不冒充执业专业人士；
- 不自动完成未经授权的外部行为；
- 对关键事实提醒用户核实；
- 对输入和输出进行必要安全处理。

---

# 第八章 支付与订单规则

## 8.1 服务端事实

- 价格从服务端套餐配置读取；
- 前端传入金额不可信；
- 在线可购买套餐使用独立白名单；
- 支付成功以服务端回调或可信查单结果为准；
- 返回页跳转不等于支付成功。

## 8.2 支付回调

必须检查：

- 签名；
- 应用和商户；
- 订单号；
- 金额；
- 支付状态；
- 交易号；
- 重复回调；
- 订单当前状态。

## 8.3 幂等与补偿

- 重复回调不重复开会员；
- 不重复发 AI 额度；
- 不重复记推广佣金；
- 支付成功但权益失败进入可补偿状态；
- 不要求用户再次付款；
- 退款保留订单和审计历史。

## 8.4 沙箱与测试

- 沙箱入口不得在生产默认开放；
- 内部测试套餐仅限授权超级管理员；
- 0.01 元测试、沙箱和 Mock 不得冒充真实商业验收；
- 测试订单与正式订单应可区分。

---

# 第九章 外部服务规则

适用于：

- 阿里云邮件推送；
- 阿里云百炼；
- 支付宝；
- 对象存储；
- 域名和证书；
- 其他第三方服务。

## 9.1 配置状态

每个外部服务应区分：

- 未配置；
- 配置不完整；
- 已配置未验证；
- 测试环境验证；
- 生产环境验证；
- 暂时异常。

## 9.2 失败处理

- 不吞掉错误返回成功；
- 错误信息不得泄露密钥；
- 可重试操作设置合理重试；
- 非幂等操作避免重复执行；
- 超时后提供可查询状态；
- 重要失败写入可审计记录。

---

# 第十章 测试与验证

## 10.1 开发中最小检查

根据修改范围执行：

- TypeScript；
- ESLint；
- Prisma validate/generate；
- 相关模块测试；
- 关键 API 测试；
- 必要的浏览器检查。

开发阶段不要求每个小改动都运行全部 E2E，但不得完全不验证。

## 10.2 合并前检查

影响业务代码的正式 PR 通常必须通过：

1. `npx prisma validate`（涉及 Prisma 时）；
2. `npx prisma generate`（涉及 Prisma 时）；
3. `npm run lint` 或仓库正式静态检查；
4. `npx tsc --noEmit`；
5. `npm run build`；
6. 与改动直接相关的测试。

若因环境缺少外部服务无法验证，必须明确说明，不得写成通过。

## 10.3 产品级验收

构建通过不等于产品完成。还需检查：

- 用户主流程；
- 权限；
- 免费/付费边界；
- 手机端；
- 支付状态；
- AI 失败状态；
- 企业隔离；
- 真实数据展示；
- 未完成功能状态。

## 10.4 响应式

重点检查：

- 360px；
- 375px；
- 390px；
- 414px；
- 430px；
- 平板；
- 桌面。

不得出现横向溢出、底栏遮挡、弹窗超屏、白字白底和按钮无法点击。

## 10.5 生产验证边界

以下事项只能在实际环境验证后才能宣称完成：

- 正式支付宝交易；
- 正式邮件发送；
- 正式百炼调用；
- 生产数据库 migration；
- PM2/Nginx；
- 域名 HTTPS；
- 生产缓存；
- 真实服务器性能。

---

# 第十一章 部署规则

## 11.1 部署前只读检查

部署 Agent 必须先只读确认：

- 目标服务器；
- 项目目录；
- Git/部署产物来源；
- PM2 名称和状态；
- 监听端口；
- Nginx；
- 数据库连接目标；
- migration 状态；
- 环境变量名称存在性；
- 待上传文件；
- 回滚点。

只读阶段不得上传、覆盖、重启、迁移或修改配置。

## 11.2 部署产物

生产部署只上传运行所需内容。

默认排除：

- `.md` 文档；
- reports；
- audits；
- PRD；
- 历史资料；
- 本地日志；
- 测试截图；
- `.env*`；
- Git 元数据；
- 无关缓存。

文档保留在 GitHub，不需要进入生产运行目录，除非程序运行明确依赖。

## 11.3 数据库部署

- 生产只使用 `prisma migrate deploy`；
- 不执行 reset；
- migration 前确认数据库名称和用户；
- 涉及高风险数据变更时先备份或快照；
- migration 失败停止后续重启；
- 不手工跳过失败 migration 冒充成功。

## 11.4 构建与启动

标准顺序按部署方案执行，通常包括：

1. 上传或更新代码；
2. 安装锁定依赖；
3. Prisma generate；
4. migrate deploy；
5. production build；
6. 重启 PM2；
7. 本机健康检查；
8. 公网页面/API 冒烟；
9. 记录部署提交和结果。

## 11.5 回滚

上线前必须明确：

- 代码回滚点；
- 数据库是否向后兼容；
- 是否有快照或备份；
- 回滚是否会丢失新数据；
- 谁批准回滚。

不得只说“可以回滚”而没有实际路径。

---

# 第十二章 Agent 协作规则

## 12.1 总控责任

多 Agent 并发时，总控 Agent 负责：

- 划分不冲突范围；
- 明确每个 Agent 的允许和禁止修改目录；
- 防止重复实现；
- 汇总差异；
- 解决规则冲突；
- 统一验证；
- 输出最终变更清单。

## 12.2 子 Agent 边界

子 Agent 不得：

- 超出任务范围；
- 覆盖其他 Agent 改动；
- 自行改产品定位；
- 自行删除未来结构；
- 自行修改生产配置；
- 未验证就宣称成功；
- 因当前不使用就删除模块。

## 12.3 并发原则

可以并发处理彼此独立的模块，但以下内容应由单一负责人统一：

- 套餐与权益；
- 数据库 Schema；
- 全局导航；
- 认证与权限；
- 支付订单状态；
- 共享设计系统；
- 公共 API 类型。

## 12.4 任务记录

比赛阶段的专用 Agent 台账不再是所有真实开发任务的强制前置条件。

当前正式任务至少需要保留：

- 目标；
- 修改范围；
- 变更文件；
- 验证结果；
- 未验证事项；
- 风险；
- Git 提交或 PR。

如参加比赛或外部审计，可在不干扰真实产品主线的前提下额外维护比赛记录。

---

# 第十三章 故障与安全事件

发生以下情况时应立即停止扩大影响：

- 错误数据库目标；
- 生产密钥泄露；
- 支付重复发权益；
- 企业数据越权；
- 免费用户产生真实 AI 成本；
- migration 破坏数据；
- 管理员权限失控；
- 部署导致网站不可用。

处理顺序：

1. 控制影响；
2. 保留证据；
3. 回滚或关闭高风险入口；
4. 修复根因；
5. 验证数据；
6. 恢复服务；
7. 形成事件记录和预防措施。

不得通过删除日志掩盖问题。

---

# 第十四章 完成声明规则

Agent 或开发人员只有在具有证据时才能使用：

- 已完成；
- 已修复；
- 已通过；
- 已上线；
- 已部署；
- 已支付成功；
- 已生产验证。

完成声明至少应说明：

- 修改文件或提交；
- 执行的命令或测试；
- 结果；
- 环境；
- 仍未验证的部分。

本地构建通过只能说明本地构建通过，不代表生产外部服务已验证。

---

# 附录 A：当前长期工程原则

1. 模块化单体优先；
2. 不提前引入不必要的 Kubernetes、Kafka 和复杂微服务；
3. 权限在服务端；
4. 套餐与权益单一来源；
5. 支付和额度可审计、可补偿；
6. 企业数据必须有 Workspace Scope；
7. 公开页与预览共用渲染；
8. 失败安全，不伪造成功；
9. 保留优先，删除需批准；
10. 文档不进入生产部署包；
11. 代码、数据库和运行结果必须与正式 PRD 一致；
12. 证据先于完成声明。

# 附录 B：当前正式文档

- `DOCUMENT_INDEX.md`
- `PRODUCT_CONSTITUTION.md`
- `PRD.md`
- `PROJECT_RULES.md`
- `docs/V2_CONSTITUTION_PRD_CODE_AUDIT_20260711.md`

其他文档是否有效，以 `DOCUMENT_INDEX.md` 和文件自身状态为准。

---

**执行结论：**

> PROJECT_RULES 只负责保证 Link168 被安全、渐进、可审计地实现。它必须服从产品宪法和 PRD，不能重新决定 Link168 要做成什么。