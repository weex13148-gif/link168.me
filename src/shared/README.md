# Shared

`src/shared` contains stable, cross-domain contracts and small framework-independent primitives.

It must not contain feature-specific workflows, React components, route handlers, database clients, provider SDKs or mutable global business state.
