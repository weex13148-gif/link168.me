# 锁定治理区变更流程

`00_GOVERNANCE_LOCKED/**` 默认为不可修改区域。

只有满足以下全部条件才能变更：

1. 老板明确提出或批准变更。
2. 记录修改原因、影响范围和兼容性风险。
3. 列出修改前后的规则差异。
4. 同步检查 PRD、页面承诺、服务端权限和验收标准。
5. 更新完整性标记。
6. 在 `05_OPERATION_LOGS/governance/` 新增变更记录。
7. 由总控 Agent 验证不存在第二份同时生效的宪法。

发现冲突时不得自行修改宪法适应旧代码，应停止冲突部分并提交老板裁决。
